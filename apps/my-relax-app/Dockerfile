# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build my-relax-app`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t my-relax-app`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system my-relax-app && \
          adduser --system -G my-relax-app my-relax-app

COPY apps/my-relax-app/dist my-relax-app/
RUN chown -R my-relax-app:my-relax-app .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix my-relax-app --omit=dev -f install

CMD [ "node", "my-relax-app" ]
